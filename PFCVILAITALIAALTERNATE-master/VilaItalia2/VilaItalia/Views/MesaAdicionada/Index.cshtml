@model IEnumerable<VilaItalia.Models.MesaAdicionada>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Disponibilidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Disponibilidade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MesaAdicionadaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MesaAdicionadaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MesaAdicionadaId })
        </td>
    </tr>
}

</table>
